# Welcome to Scorching Godot!

extends Node

var coolinteger = -69
var oi_noice = 6.9
var eh = "When not_necro on vc?"
var ever = false

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	coolinteger * -1
	oi_noice * 10
	print(eh, ever)


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
